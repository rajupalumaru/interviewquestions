const ArrayObjects = () => {
    const arr1 = [1, 2, 3, 4, 5];
    
    const arr = [{
        name: "r",
        age: 23,
        price: 44
    },
    {
        name: "ramu",
        age: 24,
        price: 45
    }, {
        name: "rahul",
        age: 25,
        price: 46
    }
    ]

    /* const newArr=arr.map(ele=>ele.price).reduce((acc,c)=>acc+c); */
    /* arr.push({name:"ramu",price:20}) */
    /* arr.pop(); */
    /* arr.unshift({name:"ramu",price:20}) */
    /*  arr.shift(); */
    /* arr.splice(1,1,{name:"ramu",price:20}) */
    /* delete arr[0].age; */
    console.log(arr);
    /* console.log(arr.map(ele=>ele));
    console.log(arr.filter(ele=>ele>4));
    console.log(arr.reduce((acc,c)=>acc+c));  */
    /* arr.push(1,2); */
    /* arr.pop(); */
    /* arr.unshift(5,6) */
    /* arr.shift(); */
    /* arr.splice(2,1); */
    /* delete arr[3] */
    /* arr.splice(4,1,9) */
    /* console.log(arr.includes(5)) */
    /* console.log(arr); */
    return (
        <div>

        </div>
    )
}
export default ArrayObjects;